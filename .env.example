# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Database Connection (PostgreSQL)
DATABASE_URL="postgresql://postgres:admin@localhost:5432/sonam_db?schema=public"

# Default Dev User ID
DEFAULT_USER_ID="user_dev_01"

# Web Push VAPID Configuration (Optional for Phase 1 Web Push)
VAPID_PUBLIC_KEY="BG...placeholder..."
VAPID_PRIVATE_KEY="...placeholder..."
VAPID_SUBJECT="mailto:dev@sonam.app"
